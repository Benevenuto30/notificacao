server:
  port: 8082
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: 'SEU_EMAIL_AQUI'
    password: 'SUA_SENHA_DE_APLICACAO_AQUI'
    protocol: smtp
    properties:
      mail:
        smtp:
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
            fallback: false
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timout: 3000
          writetimeout: 5000
  thymeleaf:
    enable: true

envio:
  email:
    remetente: 'SEU_EMAIL_AQUI'
    nomeRemetente: 'Benevenuto'